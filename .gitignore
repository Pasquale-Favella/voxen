.venv/
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
.vscode/*.log
build/
dist/
# PyInstaller generates Voxen.spec on every build; scripts/build.py is the
# canonical config, so keep the generated spec out of version control.
*.spec

# Local configuration and Whisper model caches
config.json
models/
